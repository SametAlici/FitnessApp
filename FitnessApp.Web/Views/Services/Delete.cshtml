@model FitnessApp.Web.Models.Service

@{
    ViewData["Title"] = "Hizmeti Sil";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            
            @if (ViewBag.ErrorMessage != null)
            {
                <div class="alert alert-danger d-flex align-items-center shadow-sm mb-4" role="alert">
                    <i class="bi bi-exclamation-triangle-fill fs-4 me-3"></i>
                    <div>
                        <strong>İşlem Başarısız!</strong> @ViewBag.ErrorMessage
                    </div>
                </div>
            }

            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-header bg-danger text-white text-center py-3">
                    <h3 class="mb-0 fw-bold"><i class="bi bi-trash"></i> Hizmeti Sil</h3>
                    <p class="mb-0 small opacity-75">Bu işlem geri alınamaz.</p>
                </div>
                <div class="card-body p-4 text-center bg-light">
                    <h4 class="text-dark fw-bold mb-3">@Model.Name</h4>
                    
                    <div class="row justify-content-center mb-4">
                        <div class="col-auto">
                            <span class="badge bg-secondary fs-6 p-2">⏱️ @Model.DurationMinutes Dk</span>
                        </div>
                        <div class="col-auto">
                            <span class="badge bg-success fs-6 p-2">💰 @Model.Price ₺</span>
                        </div>
                    </div>

                    <p class="text-muted fst-italic">"@Model.Description"</p>

                    <form asp-action="Delete" class="mt-4">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2">
                            <input type="submit" value="Evet, Sil" class="btn btn-danger btn-lg fw-bold" />
                            <a asp-action="Index" class="btn btn-outline-secondary">Vazgeç</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>