@model FitnessApp.Web.Models.Appointment

@{
    ViewData["Title"] = "Randevu Sil";
}

<div class="container mt-5">
    <div class="card shadow border-danger">
        <div class="card-header bg-danger text-white">
            <h3 class="mb-0">🗑️ Randevuyu Silmek İstiyor musunuz?</h3>
        </div>
        <div class="card-body">
            <p class="lead text-center text-danger">Bu işlem geri alınamaz!</p>
            <dl class="row">
                <dt class="col-sm-3">Tarih</dt>
                <dd class="col-sm-9">@Model.Date</dd>

                <dt class="col-sm-3">Üye</dt>
                <dd class="col-sm-9">@Model.Member?.UserName</dd>

                <dt class="col-sm-3">Antrenör</dt>
                <dd class="col-sm-9">@Model.Trainer?.FullName</dd>
            </dl>
            
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                    <input type="submit" value="Evet, Sil" class="btn btn-danger btn-lg px-5" />
                    <a asp-action="Index" class="btn btn-secondary btn-lg px-5">Vazgeç</a>
                </div>
            </form>
        </div>
    </div>
</div>