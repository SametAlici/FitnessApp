@model FitnessApp.Web.Models.Appointment

@{
    ViewData["Title"] = "Randevu Detayı";
}

<div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-info text-white">
            <h3 class="mb-0">📄 Randevu Bilgileri</h3>
        </div>
        <div class="card-body">
            <dl class="row fs-5">
                <dt class="col-sm-4">📅 Tarih:</dt>
                <dd class="col-sm-8">@Model.Date.ToString("dd.MM.yyyy HH:mm")</dd>

                <dt class="col-sm-4">👤 Üye:</dt>
                <dd class="col-sm-8">@Model.Member?.UserName</dd>

                <dt class="col-sm-4">💪 Antrenör:</dt>
                <dd class="col-sm-8">@Model.Trainer?.FullName</dd>

                <dt class="col-sm-4">🧘 Hizmet:</dt>
                <dd class="col-sm-8">@Model.Service?.Name</dd>

                <dt class="col-sm-4">💰 Ücret:</dt>
                <dd class="col-sm-8" class="text-success fw-bold">
                    @Model.Service?.Price.ToString("C2") </dd>
                <dt class="col-sm-4">📌 Durum:</dt>
                <dd class="col-sm-8">
                    @if(Model.Status.ToString() == "Pending") { <span class="badge bg-warning text-dark">Bekliyor</span> }
                    else if(Model.Status.ToString() == "Confirmed") { <span class="badge bg-success">Onaylı</span> }
                    else if(Model.Status.ToString() == "Cancelled") { <span class="badge bg-danger">İptal Edildi</span> }
                    else { <span class="badge bg-secondary">Tamamlandı</span> }
                </dd>
            </dl>
        </div>
        <div class="card-footer">
            <a asp-action="Index" class="btn btn-secondary">Listeye Dön</a>
        </div>
    </div>
</div>