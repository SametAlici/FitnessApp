@model IEnumerable<FitnessApp.Web.Models.Trainer>

@{
    ViewData["Title"] = "Müsaitlik Sorgula";
}

<div class="container mt-5">
    <div class="card shadow-sm border-0 mb-5">
        <div class="card-body bg-light p-4 rounded">
            <h2 class="text-center text-primary mb-4">🔍 Müsait Antrenör Bul</h2>
            
            <form asp-action="Search" method="get" class="row g-3 justify-content-center">
                <div class="col-md-4">
                    <label class="form-label fw-bold">Tarih Seçiniz</label>
                    <input type="date" name="searchDate" class="form-control" value="@ViewBag.SelectedDate" required />
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-bold">Saat Seçiniz</label>
                    <select name="searchHour" class="form-select" required>
                        <option value="">-- Saat --</option>
                        @for (int i = 7; i <= 22; i++)
                        {
                            <!option value="@i" @(ViewBag.SelectedHour == i ? "selected" : "")>@i:00</!option>
                        }
                    </select>
                </div>
                <div class="col-md-2 align-self-end">
                    <button type="submit" class="btn btn-primary w-100">Sorgula</button>
                </div>
            </form>
        </div>
    </div>

    @if (Model != null && Model.Any())
    {
        <h4 class="mb-3 text-success">🎉 Müsait Antrenörler:</h4>
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100 border-0 shadow-sm hover-card">
                        <div class="text-center mt-3">
                            <img src="@(string.IsNullOrEmpty(item.PhotoUrl) ? "https://cdn-icons-png.flaticon.com/512/8815/8815112.png" : item.PhotoUrl)" 
                                 class="rounded-circle img-thumbnail" style="width: 120px; height: 120px; object-fit: cover;">
                        </div>
                        <div class="card-body text-center">
                            <h5 class="card-title fw-bold">@item.FullName</h5>
                            <p class="text-muted">@item.Specialty</p>
                            <span class="badge bg-success">MÜSAİT</span>
                        </div>
                        <div class="card-footer bg-white text-center border-0 pb-3">
                            <a asp-controller="Appointments" asp-action="Index" class="btn btn-outline-primary btn-sm">📅 Hemen Randevu Al</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else if (Context.Request.Query.ContainsKey("searchDate"))
    {
        <div class="alert alert-warning text-center">
            😔 Üzgünüz, seçilen tarih ve saatte uygun antrenör bulunamadı.
        </div>
    }
</div>